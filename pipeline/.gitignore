# Python virtual environment
venv/
__pycache__/
*.pyc

# Environment variables
.env

# Note: We keep markdown files in output/ as they are important intermediate state
# Note: We keep JSON files in output/ if any are generated there temporarily
